<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_ergasia1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".listResTitle"></activity>
        <activity android:name=".listResDirector" />
        <activity android:name=".titleRes" />
        <activity android:name=".Search" />
        <activity android:name=".Update" />
        <activity android:name=".Delete" />
        <activity android:name=".Insert" />
        <activity android:name=".Menu" />
        <activity android:name=".ChangePass" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>